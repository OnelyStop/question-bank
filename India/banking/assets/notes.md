Charts and tables that questions genuinely need, embedded into the cleaned PDFs.

Only images classified as `chart` in `tools/assets_classified.json` live here. The
extractor also pulled out coaching-house adverts and working diagrams from the
answer key; those are not questions' data and are not kept.

Referenced from `cleaned/N.json` as `assets/<filename>`, relative to
`India/banking/`.
