__pycache__/
*.pyc
.ruff_cache/
.DS_Store
_pipeline_test_out/

# The source PDFs are committed under corpus/pdf/ — restored from the machine
# that ran the first extraction. corpus/sets/ has 20 typeset PDFs, also tracked.

# Generated by the pipeline — rebuild it, don't commit it
# data/batch*/ IS committed — regenerating it needs PyMuPDF and 414 MB of PDFs,
# so steps 2-5 work against it directly. The export is derived, so it is not.
data/*
!data/README.md
!data/batch*/
data/questions.jsonl
data/questions.jsonl.gz
pipeline/*/out/
