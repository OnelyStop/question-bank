__pycache__/
*.pyc
.ruff_cache/
.DS_Store
_pipeline_test_out/

# The source PDFs are committed under corpus/pdf/ — restored from the machine
# that ran the first extraction. corpus/sets/ has 20 typeset PDFs, also tracked.
corpus/papers/_assets/

# data/ is the pipeline's output. The extracted papers ARE committed — the team
# works on steps 2-5 against them without re-running step 1, which needs PyMuPDF
# and 414 MB of PDFs. The final export is derived from them, so it is not.
data/*
!data/README.md
!data/papers/
data/questions.jsonl
data/questions.jsonl.gz
pipeline/*/out/
